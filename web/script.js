document.addEventListener('DOMContentLoaded', () => {

    // Simulasi loading selesai (opsional)
    // Anda bisa hapus atau gunakan ini untuk pindah halaman setelah beberapa detik
    // setTimeout(() => {
    //     console.log('Loading selesai!');
    //     // window.location.href = 'halaman-selanjutnya.html';
    // }, 4000);
});
